import React from "react";
type Props = {
    className?: string;
    sectionName: string;
};
declare const PlaceholderSection: ({ className, sectionName }: Props) => React.JSX.Element;
export default PlaceholderSection;
