import React, { ReactNode } from "react";
type Props = {
    className?: string;
    src?: string;
    children?: ReactNode;
};
declare const VideoBackground: ({ className, src, children }: Props) => React.JSX.Element;
export default VideoBackground;
