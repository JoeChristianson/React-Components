import React from "react";
type Props = {
    src: string;
    alt: string;
    className?: string;
};
declare const ImageBottomDiv: ({ src, className, alt }: Props) => React.JSX.Element;
export default ImageBottomDiv;
