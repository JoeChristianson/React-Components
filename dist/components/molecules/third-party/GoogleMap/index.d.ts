import Coordinates from "types/Coordinates";
type Props = {
    className?: string;
    coordinates: Coordinates;
};
declare const GoogleMap: ({ className, }: Props) => <className = {
    fullClassName: any;
}>() => any;
export default GoogleMap;
