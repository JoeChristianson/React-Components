import React from "react";
type Props = {
    image: string;
    alt: string;
    heading: string;
    subheading: string;
    className?: string;
};
declare const FullImageCard1: ({ alt, image, heading, subheading, className }: Props) => React.JSX.Element;
export default FullImageCard1;
