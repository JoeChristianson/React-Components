import React, { ReactNode } from "react";
type Props = {
    className?: string;
    children: ReactNode;
    reverse?: boolean;
};
declare const FlexCenterer2x: ({ className, children, reverse }: Props) => React.JSX.Element;
export default FlexCenterer2x;
