import React, { ReactNode } from "react";
type Props = {
    children: ReactNode;
    className?: string;
};
declare const FlexBar: ({ className, children }: Props) => React.JSX.Element;
export default FlexBar;
