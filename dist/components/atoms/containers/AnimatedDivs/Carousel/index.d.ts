import React, { ReactNode } from 'react';
interface Props {
    slides: ReactNode[];
}
declare const FullScreenCarousel1: ({ slides }: Props) => React.JSX.Element;
export default FullScreenCarousel1;
