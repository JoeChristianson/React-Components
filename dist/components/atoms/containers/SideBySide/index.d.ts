import React, { ReactNode } from "react";
type Props = {
    className?: string;
    children: ReactNode;
};
declare const SideBySide: ({ children, className }: Props) => React.JSX.Element;
export default SideBySide;
