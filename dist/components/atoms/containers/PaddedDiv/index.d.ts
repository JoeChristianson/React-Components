import React, { ReactNode } from "react";
type Props = {
    children: ReactNode;
    className?: string;
};
declare const PaddedDiv: ({ children, className }: Props) => React.JSX.Element;
export default PaddedDiv;
