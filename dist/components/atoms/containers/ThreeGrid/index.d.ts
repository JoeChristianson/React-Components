import React, { ReactNode } from "react";
type Props = {
    children: ReactNode;
    className?: string;
};
declare const ThreeGrid: ({ children, className }: Props) => React.JSX.Element;
export default ThreeGrid;
