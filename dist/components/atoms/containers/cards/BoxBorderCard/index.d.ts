import React, { ReactNode } from "react";
type Props = {
    className?: string;
    children: ReactNode;
};
declare const BoxBorderCard: ({ className, children }: Props) => React.JSX.Element;
export default BoxBorderCard;
