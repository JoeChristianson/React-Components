import React, { ReactNode } from "react";
type Props = {
    className?: string;
    children: ReactNode;
};
declare const NavHeading: ({ className, children }: Props) => React.JSX.Element;
export default NavHeading;
