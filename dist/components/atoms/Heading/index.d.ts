import React, { ReactNode } from "react";
type Props = {
    children: ReactNode;
    className?: string;
};
declare const Heading: ({ children, className }: Props) => React.JSX.Element;
export default Heading;
