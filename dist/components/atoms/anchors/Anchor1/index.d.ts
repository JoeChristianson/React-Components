import React, { ReactNode } from "react";
type Props = {
    className?: string;
    children: ReactNode;
    href?: string;
    targetBlank?: boolean;
};
declare const Anchor1: ({ className, children, href, targetBlank }: Props) => React.JSX.Element;
export default Anchor1;
