import React from "react";
type Props = {
    className?: string;
    platform: ReviewSourceType;
};
declare const ReviewPlatformIcon: ({ className, platform }: Props) => React.JSX.Element;
export default ReviewPlatformIcon;
