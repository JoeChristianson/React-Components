import React, { ReactNode } from "react";
type Props = {
    className?: string;
    children: ReactNode;
};
declare const Section1: ({ className, children }: Props) => React.JSX.Element;
export default Section1;
