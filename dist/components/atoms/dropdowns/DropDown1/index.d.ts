import React, { ReactNode } from "react";
type Props = {
    className?: string;
    children: ReactNode;
};
declare const Dropdown1: ({ className, children }: Props) => React.JSX.Element;
export default Dropdown1;
