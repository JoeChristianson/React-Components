import React from "react";
type Props = {
    className?: string;
    src: string;
    alt: string;
};
declare const FullWidthImage: ({ className, src, alt }: Props) => React.JSX.Element;
export default FullWidthImage;
