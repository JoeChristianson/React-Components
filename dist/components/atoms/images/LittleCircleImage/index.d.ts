import React from "react";
type Props = {
    className?: string;
    img: string;
    alt: string;
};
declare const LittleCircleImage: ({ className, img, alt }: Props) => React.JSX.Element;
export default LittleCircleImage;
